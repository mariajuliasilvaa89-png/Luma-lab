import { CustomProjectStudio } from "@/components/product/CustomProjectStudio";

export const metadata = {
  title: "Personalizados"
};

export default function CustomPage() {
  return (
    <main>
      <CustomProjectStudio />
    </main>
  );
}
